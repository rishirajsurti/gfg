#include <bits/stdc++.h>
using namespace std;
typedef vector<int> vi;

bool solve(string s){
	for(int i = 0; s[i]; i++){
		string 
	}
}
int main(){
	int t; scanf("%d", &t);
	while(t--){
		string s;
		cin>>s;
		printf("%s\n", solve(s) ? "True" : "False");
	}
	return 0;
}