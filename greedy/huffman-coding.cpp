#include <bits/stdc++.h>
using namespace std;

struct node{
	int freq;
	char c;
	node *left, *right;
};

char arr[] = {'a', 'b', 'c', 'd', 'e', 'f'};
int freq[] = {5, 9, 12, 13, 16, 45};
int size;

int main(){

	size = sizeof(arr) / sizeof(arr[0]);
	
}